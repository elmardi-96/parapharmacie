{% extends 'base.html.twig' %}

{% block title %}PFornisseur{% endblock %}

{% block body %}
    <h1>PFornisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ p_fornisseur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ p_fornisseur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ p_fornisseur.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ p_fornisseur.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ p_fornisseur.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ p_fornisseur.email }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ p_fornisseur.dateCreation ? p_fornisseur.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('p_fornisseur_index') }}">back to list</a>

    <a href="{{ path('p_fornisseur_edit', {'id': p_fornisseur.id}) }}">edit</a>

    {{ include('p_fornisseur/_delete_form.html.twig') }}
{% endblock %}
